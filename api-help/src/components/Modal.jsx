import React from "react";

function Modal({}) {
  return (
    <div className={`modal `}>
      <button className="btn">close modal</button>
      <p>this is a modal</p>
    </div>
  );
}

export default Modal;
